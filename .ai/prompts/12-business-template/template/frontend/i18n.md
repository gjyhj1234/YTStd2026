# {业务名称} — 前端国际化接入

## 目标

为 {业务名称} 前端全量接入国际化。

---

## 前置阅读

- `.ai/rules/i18n.md` — 国际化规范
- `.ai/prompts/05-i18n/frontend-i18n.md` — 前端国际化通用提示词

---

## 执行步骤

1. 实现 `src/locales/runtime/t.ts`、`gt.ts`、`loader.ts`
2. 执行 `dotnet build YTStd.slnx` 生成 `locales/generated/`
3. 创建 `locales/common/` 全局复用资源
4. 为每个 `.vue` 组件创建紧贴的语言文件
5. 配置 DevExtreme 组件本地化
6. 实现语言切换功能

---

## 验收标准

- [ ] t()/gt() 实现正确
- [ ] 所有用户可见文本使用 t()
- [ ] 每个 .vue 组件有对应语言文件
- [ ] `npm run build` 通过
